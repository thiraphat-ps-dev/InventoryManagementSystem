version: '3.2'
services:
  backend:
    build: ./backend
    volumes:
      - ./backend:/app/backend
    ports:
      - '8000:8000'
    stdin_open: true
    tty: true
    command: python3 manage.py runserver 0.0.0.0:8000
